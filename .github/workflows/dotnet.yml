name: TaskFlow - Build & Test

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build-and-test:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore TaskFlow.slnx

      - name: Build
        run: dotnet build TaskFlow.slnx --no-restore --configuration Release

      - name: Run unit tests
        run: dotnet test TaskFlow.Tests/TaskFlow.Tests.csproj --no-build --configuration Release

      - name: Run integration tests
        run: dotnet test TaskFlow.IntegrationTests/TaskFlow.IntegrationTests.csproj --no-build --configuration Release